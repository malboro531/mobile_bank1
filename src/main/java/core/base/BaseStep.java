package core.base;

import static core.InitialDriver.getAndroidDriver;


//TODO не уверен! @Listeners(Listener.class)
public class BaseStep {
    protected static AndroidBasePage androidBasePage = new AndroidBasePage();
}
